[build]
builder = "dockerfile"

[deploy]
startCommand = "cd backend && python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[deploy]
startCommand = "cd backend && python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[nixpacks]
aptPkgs = ["libnss3", "libatk1.0-0t64", "libatk-bridge2.0-0t64", "libcups2t64", "libxcomposite1", "libxdamage1", "libxrandr2", "libgbm1", "libpango-1.0-0", "libcairo2", "libasound2t64", "libxshmfence1", "libxkbcommon0", "libdrm2"]
